-- Уникальный ключ по (марка, модель, годы) мешает нескольким поколениям.
--
-- У Civic бывает и хэтчбек, и седан в пересекающиеся годы — это разные
-- строки с разными проводками. Строки теперь правятся по id, поэтому
-- жёсткий ключ только вредит: при добавлении второго поколения запись
-- молча перезаписывала бы первое.
DROP INDEX IF EXISTS t_p61203256_auto_catalog_site.vehicle_wiring_key;